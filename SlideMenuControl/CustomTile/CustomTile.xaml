<UserControl x:Class="SlideMenuControl.CustomTile.CustomTile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:blake="clr-namespace:Blake.NUI.WPF.Gestures;assembly=Blake.NUI.WPF"
             mc:Ignorable="d" Height="Auto" Width="Auto">
    
    <UserControl.Resources>
        <Storyboard x:Key="shrinkTile">
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.1" />
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.1" />
        </Storyboard>
        <Storyboard x:Key="expandTile">
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.1" Duration="0:0:0.1" />
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.1" Duration="0:0:0.1" />
        </Storyboard>
    </UserControl.Resources>
    
    <StackPanel Name="StackBorder" >
        <StackPanel Orientation="Vertical" Name="StackTile" RenderTransformOrigin="0.5,0.5">
            <StackPanel.RenderTransform>
                <ScaleTransform ScaleX="1" ScaleY="1" />
            </StackPanel.RenderTransform>
            <Image Name="iconTitle" Margin="0,0,0,0" Height="120" Width="120" 
                   TouchDown="iconTitle_TouchDown" TouchUp="iconTitle_TouchUp" TouchLeave="iconTitle_TouchLeave"
                   MouseLeftButtonDown="iconTitle_MouseLeftButtonDown" MouseLeftButtonUp="iconTitle_MouseLeftButtonUp"
                   MouseLeave="iconTitle_MouseLeave" MouseEnter="iconTitle_MouseEnter">
                <Image.Style>
                    <Style TargetType="{x:Type Image}">
                        <Setter Property="Cursor" Value="Arrow" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Cursor" Value="Hand" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>
            <Label Name="lblTitle" Height="Auto" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Center">
            </Label>
        </StackPanel>
    </StackPanel>
</UserControl>